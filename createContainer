pipeline {
    agent any
    stages {
        stage('Create coteneur'){
            steps{
                sh 'sudo docker rm -f myapp-image'
                sh 'sudo docker run -d --name myapp --hostname myapp myapp-image:latest'
                sh 'sudo docker ps'
            }
        }


    }
}
